import React from 'react';

import styles from './styles.module.css';

export default function PlaceholderImg(width, height) {
    return (
        // <div className={styles.placeholderImg} style={{ width: width, height: height }}>
        <div className={styles.placeholderImg} />
    );
}